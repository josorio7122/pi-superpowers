// Placeholder — this test is fully rewritten in Task 6 when tool.ts is rebuilt
// on pi-agents primitives. Leaving a single passing test keeps the suite green
// during the in-flight migration.
import { describe, expect, it } from "vitest";

describe("subagents tool placeholder", () => {
	it("will be rewritten in Task 6", () => {
		expect(true).toBe(true);
	});
});
